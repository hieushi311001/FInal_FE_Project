const routes = {
  home: "/",
  following: "/following",
  profile: "/profile/:profile_id",
  // profile: "/profile",
  upload: "/upload",
  search: "/search",
  login: "/login",
  register: "/register",
  productdetail: "/product/:product_id",
};

export default routes;
