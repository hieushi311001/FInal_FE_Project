const routes = {
  home: "/",
  following: "/following",
  shop: "/shop",
  profile: "/profile/:profile_id",
  // profile: "/profile",
  upload: "/upload",
  search: "/search",
  login: "/login",
  register: "/register",
  productdetail: "/product/:product_id",
  cart: "/cart",
  test: "/test",
  check_out: "/check_out",
  payment: "/payment",
};

export default routes;
