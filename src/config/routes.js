const routes = {
  home: "/",
  following: "/following",
  shop: "/shop",
  profile: "/profile/:profile_id",
  // profile: "/profile",
  upload: "/upload",
  search: "/search",
  login: "/login",
  register: "/register",
  productdetail: "/product/:product_id",
};

export default routes;
